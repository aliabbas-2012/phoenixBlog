<h4><%= link "Upload new image", to: image_path(@conn, :new) %></h4>


<%= for image <- @images do %>
  <img src='<%= BlogTest.Avatar.url({image.image, image}) |> correct_image_path %>'><br>
  <img src='<%= BlogTest.Avatar.thumb_url(image)  %>'><br>
<% end %>
